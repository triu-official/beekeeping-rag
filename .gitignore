# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.pyverse
.hypothesis/
.pytest_cache/
cover/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Output directory for the RAG pipeline project evaluations
# We are asked to commit the output directory files, so we SHOULD NOT ignore it.
# BUT we should ignore temporary files inside it if any.
# Actually the prompt says "Generate report files in output/". "Deliverables: output/evaluation_report.json, output/evaluation_report.md"
# So I won't put `output/` in .gitignore to make sure it can be submitted.

# IDEs
.vscode/
.idea/
*.swp
*.swo
